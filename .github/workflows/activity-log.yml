name: Update Activity Log

on:
  schedule:
    # 🕒 Scheduling rationale:
    # 根據日常使用習慣設定四個更新時段（台灣時間 UTC+8）
    # - 午夜 12:00（前一天 16:00 UTC）
    # - 早上 08:00（00:00 UTC）
    # - 中午 13:00（04:00 UTC）
    # - 下午 17:00（09:00 UTC）
    # 這樣可確保每日四個時段自動同步活動紀錄。
    - cron: "0 16 * * *"   # 午夜 12:00 台灣時間
    - cron: "0 0 * * *"    # 早上 8:00 台灣時間
    - cron: "0 5 * * *"    # 中午 12:00 台灣時間
    - cron: "0 9 * * *"    # 下午 5:00 台灣時間
  workflow_dispatch:          # 可手動觸發

jobs:
  update-activity:
    runs-on: ubuntu-latest

    permissions:
      contents: write         # 允許推送 README 更新

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Generate GitHub Activity
        uses: Readme-Workflows/recent-activity@main
        with:
          GH_USERNAME: "Isforuse"                       # 你的 GitHub 帳號
          CONFIG_FILE: ".github/recent-activity.config.yml" # 設定檔
        env:
          GITHUB_TOKEN: ${{ secrets.REPO_TOKEN }}            # ✅ 改用 classic PAT（名稱 TOKEN）

      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add README.md
          git commit -m "📝 chore: Update README.md with latest activity" || echo "No changes"
          git push
