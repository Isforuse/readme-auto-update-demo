name: Update Activity Log

on:
  schedule:
    # 每天早上 8 點、正午 12 點、下午 1 點、下午 5 點執行（台灣時間）
    # GitHub Actions 使用 UTC 時區，要將台灣時間減去 8 小時
    # 所以：08:00 → 00:00, 12:00 → 04:00, 13:00 → 05:00, 17:00 → 09:00
    - cron: "0 0 * * *"   # 08:00 台灣時間
    - cron: "0 4 * * *"   # 12:00 台灣時間
    - cron: "0 5 * * *"   # 13:00 台灣時間
    - cron: "0 9 * * *"   # 17:00 台灣時間
  workflow_dispatch:          # 可手動觸發

jobs:
  update-activity:
    runs-on: ubuntu-latest

    permissions:
      contents: write         # 允許推送 README 更新

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Generate GitHub Activity
        uses: Readme-Workflows/recent-activity@main
        with:
          GH_USERNAME: "Isforuse"                       # 你的 GitHub 帳號
          CONFIG_FILE: ".github/recent-activity.config.yml" # 設定檔
        env:
          GITHUB_TOKEN: ${{ secrets.REPO_TOKEN }}            # ✅ 改用 classic PAT（名稱 TOKEN）

      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add README.md
          git commit -m "📝 chore: Update README.md with latest activity" || echo "No changes"
          git push
