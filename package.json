{
  "name": "demo-project",
  "version": "1.0.0",
  "description": "A minimal project for CI/CD testing",
  "scripts": {
  "test": "node test.js",
  "build": "echo '🏗️ Building project...' && mkdir -p dist && echo 'build output' > dist/output.txt"
}
}
